# Lumix Landing (Next.js)

Minimal Next.js landing page for a service similar to Netvisor.


Quick start:

```bash
npm install
npm run dev
```

Open http://localhost:3000

Notes:
- This project was converted to TypeScript and includes Tailwind CSS.
- If you see type errors, run `npm run type-check` to inspect them.
